import React from 'react';
import { Link } from 'react-router-dom';

const Navbar = () => {
    return (
        <nav style={{ backgroundColor: '#f8f9fa', padding: '10px 0', textAlign: 'center' }}>
            <h1 style={{ margin: 0 }}>Triplanner</h1>
        </nav>
    );
};

export default Navbar; 